public int findMostPairs(Factors f) {
    ArrayList<FactorsPair> fa1 = this.pairs();
    ArrayList<FactorsPair> fa2 = f.pairs();

    if (fa1.size() == fa2.size()) {return -1;}
    else if (fa1.size()>fa2.size()) {return this.number;}
    else {return f.number;}
}