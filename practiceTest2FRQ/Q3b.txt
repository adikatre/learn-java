public int findMostPairs(Factors f) {
    ArrayList<FactorsPair> fa1 = f.buildArrayList(this.number);
    ArrayList<FactorsPair> fa2 = f.buildArrayList(number);

    if (fa1.size() == fa2.size()) {return -1}
    else if (fa1.size()>fa2.size()) {return fa1.size()}
    else {return fa2.size()}
}